interface:
  display_name: "Sol Planner with Luna or Terra"
  short_description: "Sol plans; Luna or Terra implements."
  default_prompt: "Use $sol-plan-implement to plan with Sol, ask for approval, then route implementation to Luna or Terra."

policy:
  allow_implicit_invocation: true
